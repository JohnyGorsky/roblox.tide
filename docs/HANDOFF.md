# Handoff — where we left off

**Snapshot: 2026-08-20.** Pick up here. Regenerate context from
[BUILD-STATUS.md](../BUILD-STATUS.md) and [docs/build/](build/README.md).

---

## 🔴 At risk right now: the ocean is unsaved

The sea exists in the **game place** but only as **unsaved Edit-mode changes**. Closing Studio without
saving loses all of it. Claude cannot save the place — there is no save capability in the Studio MCP.

**You must press `Ctrl+S` in the game place** (or `File → Save to Roblox` to publish).

What is at stake in that unsaved state:

| Change | Where |
|---|---|
| 6144 × 6144 terrain ocean, water at Y=0, sand seabed −64…−56 | `Workspace.Terrain` |
| Default `Baseplate` deleted | `Workspace` |
| `SpawnLocation` raised to Y=8 | `Workspace` |
| Lighting: Light Swell state, fog, atmosphere, `ExposureCompensation −0.05` | `Lighting` |
| Default tropical sky parked as `Sky_DefaultTropical_PARKED` | `ServerStorage` |
| Place settings from job 004 (streaming, camera zoom, orientation, water props) | place properties |

`SeaStates.luau` is **safe** — it is on disk in `studio_game/ReplicatedStorage/` and committed.

Everything in `roblox.tide` is committed. `roblox.workspace` has two uncommitted files (a skill edit and
finding 0001).

---

## What exists

**Infrastructure — done and verified**

- Two places, both synced: `studio_lobby/` → The Last Tide Lobby `91870148721134`,
  `studio_game/` → The Last Tide Game `100885379547959`. Layout is **flat** (`ReplicatedStorage/`,
  `ServerScriptService/`, `StarterPlayerScripts/`, … at the sync root).
- File suffix decides script class: `.luau` → ModuleScript, `.server.luau` → Script/Server,
  `.client.luau` → Script/Client. `.module.luau` is **not** a suffix.
- Place settings baseline applied and audited — [settings-baseline.md](systems/places/settings-baseline.md),
  drift check via `tools/audit-place-settings.luau`.
- Crew size confirmed on the Creator Hub: game 6, lobby 20.
- Build board generated by `tools/build-status.py`; graybox register + `tools/audit-graybox.luau`.

**Design — done**

- The full build manifest: [docs/build/](build/README.md) — ~600 items in 12 groups.
- 13 accepted decisions, 17 system docs, 11 features.

**Game content — one feature in flight**

- `GAME-0011` Sea & Sea States: `IN_PROGRESS`. Ocean built and verified; five sea states in
  `SeaStates.luau`; look **not yet approved**, and blocked on sky assets.

---

## Waiting on you

| # | What | Why it matters |
|---|---|---|
| 1 | **Save the game place** | Otherwise the ocean is lost |
| 2 | Judge the sea screenshots | Shape and horizon work; colour does not |
| 3 | **finding 0006** — source overcast sky assets | Proven blocker on the whole art direction |
| 4 | **finding 0004** — game place is `Fully Open` | A stranger could deep-link into a running expedition |
| 5 | **finding 0005** — Social Slots on the game place | A friend could drop into a 6-slot crew mid-run |
| 6 | todo 0003 — measure `LightingStyle = Realistic` on a phone | You chose the expensive path; cost unmeasured |
| 7 | todo 0001 — decide whether a `tide-style` skill is wanted | Ground rules want a per-game design skill |
| 8 | todo 0002 — re-probe `StarterGui`/`StarterPack`/`Workspace` sync | Recorded as non-synced but unconfirmed |

---

## Recommended next move

**Source the overcast skies first** (finding 0006), because group 01 cannot be finished without them and
everything else in the sea look is already done. Creator Store search is the fast route — Claude can
present candidates for approval, per the asset policy.

**Then** either:

- **Finish group 01** — wave-height sampler, so the boat has a sea to agree with; or
- **Start group 02 job 1** — vessel foundation: the module kit plus the starter hull at correct
  dimensions, floating and steering. This is the first thing that makes the game playable at all.

The manifest recommends 02 job 1 next, because a boat on the sea is the first moment the game exists.

---

## Design questions still blocking work

From the manifest's open questions — these need answers before the affected groups can proceed:

1. **What happens when the storm catches you?** Damage, forced movement, or run-ending. The storm's whole
   authority rests on this. Blocks group 07.
2. **Is one humanoid R15 rig shared** across players, NPC crew, pirates and drowned? Changes the animation
   count by roughly a third. Blocks groups 05, 10, 11.
3. **How is an island template stored?** Terrain regions are large binary data. Blocks group 04 entirely.
4. **Deck pathfinding on a moving vessel** — Roblox pathfinding assumes a static navmesh. Blocks boarding
   enemies and NPC crew.
